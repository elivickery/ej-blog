<h3>Post a Comment</h3>

<%= form_for [ @article, @comment ] do |f| %>
  <div>
    <%= f.label :author_name %><br/>
    <%= f.text_field :author_name %>
  </div>
  <div>
    <%= f.label :body %><br/>
    <%= f.text_area :body %>
  </div>
  <div>
    <%= f.submit 'Submit' %>
  </div>
<% end %>
